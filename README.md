# Upride React Internship Task

## Quick Start

- Install dependencies
    ```
    npm install
    ```

- Start development server
    ```
    npm start
    ```
- **Note:** There is an environment variable ```REACT_APP_DATA_API``` that stores the API endpoint to get bookings data.